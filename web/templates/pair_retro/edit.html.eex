<h2>Edit retrospective</h2>

<%= render "form.html", changeset: @changeset,
                        action: pair_retro_path(@conn, :update, @pair_retro),
                        projects: projects_for_select(@conn) %>

<%= link "Back", to: pair_retro_path(@conn, :index) %>
