<h2>New project</h2>

<%= render "form.html", changeset: @changeset,
                        action: admin_project_path(@conn, :create),
                        groups: @groups, users: @users %>

<%= link "Back", to: admin_project_path(@conn, :index) %>
