<h2>Users</h2>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for user <- @users do %>
    <tr>
      <td><%= user.name %></td>
      <td><%= user.email %></td>
      <td><%= active_text(user) %></td>

      <td class="text-right">
        <%= link "Show", to: user_path(@conn, :show, user), class: "btn btn-default btn-xs" %>
        <%= link "Edit", to: user_path(@conn, :edit, user), class: "btn btn-default btn-xs" %>
        <%= if user.active do %>
          <%= link "Deactivate", to: user_deactivate_path(@conn, :deactivate, user), method: :put, class: "btn btn-default btn-xs" %>
        <% else %>
          <%= link "Activate", to: user_activate_path(@conn, :activate, user), method: :put, class: "btn btn-default btn-xs" %>
        <% end %>
        <%= link "Delete", to: user_path(@conn, :delete, user), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "Back", to: page_path(@conn, :index), class: "padded-link" %>
<%= link "New user", to: user_path(@conn, :new) %>
